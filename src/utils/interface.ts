export interface IPayloadJWT {
    name: string;
    email: string;
    role: string;
}

export interface IUser {
    role: number;
    address: number;
    address_detail: string;
    phoneNumber: string;
    code: string;
    email: string;
    password: string;
}
